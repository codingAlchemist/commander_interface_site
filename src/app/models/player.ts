export class Player {
    constructor(public name:string,public password: string, public nickname:string,public desc:string, public level:number, public points: number, public email:string){

    }
}

