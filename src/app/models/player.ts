export class Player {
    constructor(public id:number = 0,public name:string,public password: string, public nickname:string,public desc:string, public level:number, public points: number, public email:string){

    }
}
