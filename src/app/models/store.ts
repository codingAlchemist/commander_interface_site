export class Store{
    constructor(public id:number, public ownerId:number, public name:string, public street:string, public city:string, public state:string, public zip:string, public logo:string, public store_number:string){

    }
}